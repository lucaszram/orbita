# Local tooling and repository metadata
.git/
node_modules/
.pnpm-store/
.expo/
.expo-shared/

# Local environment and logs
.env
.env.*
!.env.example
*.log
.DS_Store
*.tsbuildinfo

# Generated/native outputs
android/
ios/

# `android/` e `ios/` no llevan barra inicial, así que matchean un directorio con
# ese nombre a CUALQUIER profundidad — incluido `modules/<mod>/ios/`, donde vive
# el puente Apple del módulo local. Sin esta reexclusión el podspec y el Swift
# quedaban fuera del repositorio (y del tarball de EAS): la suite seguía en verde
# y el build nativo salía sin el módulo.
!modules/**/ios/

dist/
# Exports temporales de las auditorías nativas: se generan para auditar el
# bundle y se retiran en la misma corrida. `.gitignore` ya los excluía, pero EAS
# NO lee `.gitignore` cuando existe este archivo, así que sin estas dos líneas
# un export a medio limpiar viajaba dentro del tarball del build.
dist-ios/
dist-android/
web-build/

# Evidencia local de auditoría: capturas, logs, comparaciones y herramientas de
# la recertificación. Son cientos de megas que existen sólo en esta máquina y no
# forman parte del producto. NO se borra —vive fuera de git a propósito— pero
# tampoco tiene por qué subir a un servidor de builds.
.local/

# Asset archive material not required by the app bundle
assets/orbita/core/figma-import-previews/
assets/orbita/core/figma-mini-previews/
assets/orbita/core/figma-previews/

assets/orbita/higgsfield/archive-7/inbox/
assets/orbita/higgsfield/archive-7/rejected/
assets/orbita/higgsfield/archive-7/contact-sheets/
assets/orbita/higgsfield/archive-7/selected/home/

assets/orbita/higgsfield/archive-9/

assets/orbita/higgsfield/archive-10/inbox/
assets/orbita/higgsfield/archive-10/rejected/
assets/orbita/higgsfield/archive-10/needs-review/
assets/orbita/higgsfield/archive-10/contact-sheets/
