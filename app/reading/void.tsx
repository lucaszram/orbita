import { VoidExperience } from "@/components/void/VoidExperience";

/** Ruta El Vacío (deep-link / navegación con back). El tab vive en (tabs)/vacio. */
export default function VoidScreen() {
  return <VoidExperience showBack />;
}
