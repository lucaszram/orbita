/**
 * Wrapper de ruta. La implementación vive FUERA de `app/`, en
 * `src/routes/v492/tabs-umbral` (`.tsx` nativo / `.web.tsx` web): en nativo la
 * experiencia canónica del Umbral; en web, la misma más Tarot (CORE-247).
 */
export { default } from "@/routes/v492/tabs-umbral";
