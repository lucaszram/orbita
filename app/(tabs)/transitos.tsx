// Wrapper de ruta. La pantalla canónica vive en `@/screens/TransitosScreen` y la comparten
// nativo y web: no hay una versión por plataforma.
export { TransitosScreen as default } from "@/screens/TransitosScreen";
