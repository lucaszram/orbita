// Wrapper de ruta. La pantalla canónica vive en `@/screens/HomeScreen` y la comparten
// nativo y web: no hay una versión por plataforma.
export { HomeScreen as default } from "@/screens/HomeScreen";
