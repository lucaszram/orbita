import { OnboardingFlow } from "@/onboarding/OnboardingFlow";

/** Preview route for the rebuilt (clean, dark-immersive) onboarding. */
export default function OnboardingNextRoute() {
  return <OnboardingFlow />;
}
