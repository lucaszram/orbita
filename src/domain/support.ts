/**
 * Contacto público de soporte de Órbita.
 *
 * Vive acá y no dentro de una pantalla porque lo publican varias superficies
 * (Soporte, Privacidad, Términos y el paywall) y un mail distinto en cada una es
 * una promesa rota: el que escribe a la dirección vieja no recibe respuesta.
 */
export const SUPPORT_EMAIL = "soporte@orbitaastrologia.xyz";

/** Enlace listo para `Linking.openURL`. */
export const SUPPORT_MAILTO = `mailto:${SUPPORT_EMAIL}`;
