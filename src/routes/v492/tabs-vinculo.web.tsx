/**
 * Vínculos — la lista de personas y el alta (CORE-212…236).
 * Variante WEB: la implementación es la de main (reconciliación CORE-247);
 * la nativa vive en `tabs-vinculo.tsx`.
 */
import { VinculosScreen } from "@/screens/VinculosScreen";

export default function VinculoTab() {
  return <VinculosScreen />;
}
