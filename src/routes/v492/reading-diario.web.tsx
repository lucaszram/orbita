// Wrapper de ruta. La pantalla canónica vive en `@/screens/DiarioScreen` y la comparten
// nativo y web: no hay una versión por plataforma.
export { DiarioScreen as default } from "@/screens/DiarioScreen";
