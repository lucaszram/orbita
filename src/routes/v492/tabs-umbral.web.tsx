import { UmbralSections } from "@/components/web/umbral-sections";

/**
 * El Umbral (web): Preguntar —la experiencia canónica— más Tarot, con su
 * selector. El shell lo aporta el layout del grupo; acá no se vuelve a montar.
 */
export default function UmbralTab() {
  return <UmbralSections />;
}
